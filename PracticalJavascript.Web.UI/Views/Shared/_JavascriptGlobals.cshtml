@using PracticalJavascript.Web.UI.Models

<script type="text/javascript">
  (function(p, $, undefined) {
    p.virtualRoot = '@Url.Content("~")';
    
    p.storeType = p.storeType || {};
    
    p.storeType.fuel = @((int)StoreType.Fuel);
    p.storeType.maintenance = @((int)StoreType.Maintenance);
    p.storeType.fuelAndMaintenance = @((int)StoreType.FuelAndMaintenance);

  }(window.pjs = window.pjs || {}, jQuery));
</script>